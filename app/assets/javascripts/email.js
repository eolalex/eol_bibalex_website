function myFunction(){
     var email = document.getElementById('value_id').value;
     // var auth = $('.temp_information').data('temp');
     // var auth = document.getElementById('products').data('temp');
     var auth = document.getElementById("hidden_id").data('temp');
     alert(auth);
     // $.ajax({
      // url:Routes.omniauth_path(email)
// });

// $.ajax({
      // url: Routes.omniauth_path(email),
      // // parameters: {
      // // // auth: auth,
      // // email: email
    // // },
      // success: function(result) {
// }
//  
// });

     // new Ajax.Request(omniauth_path(), {
    // method: 'post',
    // parameters: {
      // menu_id: email
    // }
  // });
  
   }
