= form_with model: @collection, method: :post, local: true do |form|

  .form-group
    = form.label :name
    = form.text_field :name, required: true
    %br
  .form-group
    = form.label :description
    = form.text_field :description
    %br
  .actions
    = form.submit
