%p Add to Existing collection

%table
  %tbody
    - @collection.each do |collection|
      %tr
        %td= collection.name
        %td= link_to '    Add To Collection     ', pages_index_url
        %td= link_to 'Edit      ', edit_collection_path(collection)
        %td= link_to 'Remove Collection', collection, method: :delete, data: { confirm: 'Are you sure?' }
%br/


%li= link_to 'Create New Collection', new_collection_path