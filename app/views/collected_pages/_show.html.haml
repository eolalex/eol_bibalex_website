%body  
  %header  
    .wrapper.clearfix       
      %br
      = render("collections/header")
      = form_tag(collected_pages_path(),method: :get) do
        = text_field_tag :canonical_form 
        = hidden_field_tag('collection_id', @collection.id)
        = submit_tag 'Search'