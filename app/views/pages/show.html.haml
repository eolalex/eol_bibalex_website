= render("overview")
= render("tabs")
- if @page.medium
  = @page.scientific_name
  %br
  %div{:class => "medium"}
    %img{src: @page.medium.base_url}
    %br
    = @page.medium.owner
  - limit ||= 20
  - node ||= @page.native_node || @page.nodes.first
  - if node.children.any?
    %h4= @page.scientific_name+" includes"
    - node.children[0..limit-1].each do |child|
      =link_to child.scientific_name, page_path(child.page)
